# Hawala v2 — pinned dependencies
# Pin minor versions to prevent silent breakage on major library updates.
# Run: pip install -r requirements.txt

growwapi

# Core data
pandas>=2.0,<3.0
numpy>=1.24,<2.0
scipy>=1.11,<2.0

# Visualisation
matplotlib>=3.7,<4.0

# ML (used in options simulation / future use)
scikit-learn>=1.3,<2.0

# Market data
yfinance>=0.2.40,<0.3
nselib>=0.1.0

# Excel / file I/O
xlrd>=2.0.1

# PDF generation (gen_report.py)
reportlab>=4.0,<5.0
